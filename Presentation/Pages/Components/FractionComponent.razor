<MathJaxComponent>
    @Fraction.ConvertToMathJax(Display)
</MathJaxComponent>

@code {
    [Parameter]
    public Fraction Fraction { get; set; }

    [Parameter]
    public Display Display { get; set; } = Display.Block;
}